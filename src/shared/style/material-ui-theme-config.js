import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#469A10",
    },
    secondary: { main: "#113E87", contrastText: "#fff" },
  },
});

export default theme;
